# 是否默认启用前端 Mock 层。
# main 分支上 /api/admin/* 与 /v1/chat/completions 仍返回 501（C 在 origin/qjh 未合并，B 未实现），
# 所以开发期默认开启 Mock。B/C 合并后改为 false，或直接在页面右上角开关切换。
VITE_USE_MOCK=true

# 后端地址（供 vite.config.ts 的 dev proxy 使用）
VITE_BACKEND_ORIGIN=http://127.0.0.1:8000
